version: '3.4'

services:
  iron-exalt:
    image: iron-exalt
    volumes:
      - ./samples/iron-exalt/data/:/app/data
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 2222:2222
